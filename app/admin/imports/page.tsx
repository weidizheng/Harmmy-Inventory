import { Shell } from "../../../components/shell";
export default function ImportsPage() { return <Shell title="Import review"><section className="panel"><h2>Workbook inspection only</h2><p>Local reports are generated from the read-only workbook. Items marked NEEDS_REVIEW require a human decision and no preview is imported.</p><button>Open local report folder</button></section></Shell>; }
